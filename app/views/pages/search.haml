.col-xs-12.nudge-down
	%table.table.table-hover.table-striped.table-bordered
		%thead
			%tr
				%td Server
				%td Name
				%td Race
		%tbody
			-@players.each do |player|
				%tr(class="#{player.is_guilty? ? 'danger' : player.is_innocent? ? 'success' : 'warning' }")
					%td
						%img(src="/img/#{get_server_abbr[player.server.to_sym]}.png")
						#{player.server}
					%td
						%a(href="/search/#{player.id}/games") #{player.name}
					%td
						%img(src="#{get_race_image player.race}")
						#{player.race}
