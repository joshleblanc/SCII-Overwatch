.col-xs-12.nudge-down
	-if @players.length > 0
		%table.table.table-hover.table-striped.table-bordered
			%thead
				%tr
					%td Server
					%td Name
			%tbody
				-@players.each do |player|
					%tr
						%td
							%img(src="/img/#{get_server_abbr[player.server.to_sym]}.png")
							#{player.server.upcase}
						%td
							%a(href="/players/#{player.id}/games") #{player.name}
	-else
		%p Whoops, we couldn't find anyone matching that player.
